/*⚠️ NO MODIFIQUES EL NOMBRE DE LAS DECLARACIONES ⚠️*/
/*1️⃣ EJERCICIO 01 1️⃣*/

// En los siguientes ejercicios deberás reemplazar el valor null
// por el correspondiente.

// Crea una variable de tipo string.
const nuevoString = 'Polo';
// creo una variable llamada tipo y de tipo string por que la pongo entre las comillas
//var tipo = 'String';


// Crea una variable de tipo number.
const nuevoNumero = 5;
//var x = 5;

// Crea una variable de tipo boolean.
const nuevoBoolean = true;
// si es falso o verdadero
//var meguastProgramar = true;

var megustaProgramar = false;

// Resuelve el siguiente problema matemático.
const nuevaResta = 10 - 7 === 3;
//La respuesta es 7
//nuevaResta (7);


// Resuelve el siguiente problema matemático.
const nuevaMultiplicacion = 10 * 4 === 40;
//La respuesta es 4


// Resuelve el siguiente problema matemático.
const nuevoModulo = 21 % 5 === 1;
// la respuesta es 1.
//nuevoModulo (1);

/*⚠️ NO MODIFIQUES NADA DEBAJO DE ESTO ⚠️*/
module.exports = {
   nuevoString,
   nuevoNumero,
   nuevoBoolean,
   nuevaResta,
   nuevaMultiplicacion,
   nuevoModulo,
};
